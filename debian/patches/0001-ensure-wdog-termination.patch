--- a/src/glb_wdog_exec.c.orig
+++ b/src/glb_wdog_exec.c
@@ -296,7 +296,7 @@ init_error:
 
 //cleanup:
 
-    if (pid > 0 && !glb_terminate) {
+    if (pid > 0) {
         assert(std_in);
         assert(std_out);
 
